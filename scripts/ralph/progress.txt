## Codebase Patterns
- Delete ErpIntegration before Manufacturer in seed cleanup (FK constraint)
- seed-helpers.ts returns `{ erpSyncedBatch, missingBatch, manufacturer }` — destructure in seed.ts
- Tests hit real SQLite via libsql adapter — no mocking for DB schema tests
- Seed must include PROCESSING batch (3 total) to satisfy seed-batches.test.ts
- Recursive TypeScript types: use an intermediate interface to break circular alias (e.g. XmlRpcObject interface + XmlRpcValue type union)

# Ralph Progress Log
Started: Sun May 17 08:02:03 CEST 2026
---

## 2026-05-17 08:05 - US-ERP-001
- Added ErpIntegration model to prisma/schema.prisma with @unique manufacturerId back-relation
- Added erpIntegration field to Manufacturer model
- Ran migration: 20260517060255_add_erp_integration
- Added seedErpIntegration helper (enabled=false, type='ODOO') in seed-helpers.ts
- Updated seed.ts to call seedErpIntegration after manufacturer created
- Added PROCESSING batch to seed (was missing; seed-batches.test.ts expected 3 batches)
- Added erpIntegration.deleteMany() before manufacturer.deleteMany() in cleanup
- Wrote src/tests/erp-integration-schema.test.ts
- Files changed: prisma/schema.prisma, prisma/seed-helpers.ts, prisma/seed.ts, src/tests/erp-integration-schema.test.ts, prisma/migrations/
- **Learnings for future iterations:**
  - FK order in deleteMany: erpIntegration → order → manufacturer (not just manufacturer)
  - seed-helpers.ts must return manufacturer for seedErpIntegration call in seed.ts
  - Pre-existing failing test in seed-batches.test.ts required adding PROCESSING batch to seed
---

## 2026-05-17 08:15 - US-ERP-003
- Created src/lib/erp/sync-service.ts: syncBatchToErp(batchId) with ERP_SYNC_ENABLED guard as first line
- Queries batch with order.manufacturer.erpIntegration + passport includes
- Returns skipped:true when env unset, integration null, or integration.enabled=false
- On adapter success: updates batch status='ERP_SYNCED', erpSyncedAt, erpPayloadJson
- Created src/tests/sync-service.test.ts: 5 tests covering all guard paths + success path
- Used vi.mock for '@/lib/prisma' and '@/lib/erp/adapter-factory'
- Files changed: src/lib/erp/sync-service.ts, src/tests/sync-service.test.ts
- **Learnings for future iterations:**
  - vi.mock at module level, imports after — required for ESM mock hoisting in vitest
  - Cast mocked prisma as `unknown as { batch: { findUnique: ReturnType<typeof vi.fn> } }` for type-safe mock access
  - delete process.env.VAR in beforeEach/afterEach cleanly resets env for each test
---

## 2026-05-17 - US-ERP-005
- Added syncBatchToErp call in submitBatch after batch.update, gated on finalStatus === 'COMPLETE'
- Imported syncBatchToErp from '@/lib/erp/sync-service' in supplier upload actions.ts
- Updated sync-service.test.ts: added timing assertion (elapsed < 5ms) to the ERP_SYNC_ENABLED-unset test
- All 42 tests pass; dev flow unaffected (ERP_SYNC_ENABLED unset = immediate skipped return)
- Files changed: src/app/supplier/upload/actions.ts, src/tests/sync-service.test.ts
- **Learnings for future iterations:**
  - Date.now() timing assertion works in Vitest node env for sub-5ms guard checks
  - The sync guard returns synchronously (no await DB / network) so timing < 5ms is reliable
---

## 2026-05-17 - US-ERP-004
- Created src/app/manufacturer/integrations/page.tsx: async server component, queries manufacturer with erpIntegration include, passes to IntegrationForm
- Created src/app/manufacturer/integrations/IntegrationForm.tsx: 'use client', renders status badge + save form + separate delete form
- Created src/app/manufacturer/integrations/actions.ts: saveIntegration (upsert) + deleteIntegration server actions
- Added 'Integrations' nav link (with Plug icon) to ManufacturerDashboard header badge row
- Files changed: src/app/manufacturer/integrations/page.tsx, IntegrationForm.tsx, actions.ts, src/components/manufacturer/ManufacturerDashboard.tsx
- **Learnings for future iterations:**
  - Nested forms are invalid HTML — split save and delete into sibling forms, not nested
  - deleteIntegration.bind(null, id) works cleanly as a form action for parameterized server actions
  - No manufacturer layout.tsx exists; nav links added inline to ManufacturerDashboard header badges area
---

## 2026-05-17 08:10 - US-ERP-002
- Created src/lib/erp/types.ts: ErpPayload, ErpSyncResult, ErpAdapterConfig, ErpAdapter interface
- Created src/lib/erp/odoo-adapter.ts: OdooAdapter with buildXmlRpcCall helper, XML string template serialization, string-based XML parsing
- Created src/lib/erp/adapter-factory.ts: getAdapter() factory returning OdooAdapter for type 'ODOO'
- Created src/tests/odoo-adapter.test.ts: 3 tests covering authenticate uid parsing, execute_kw success, fetch error handling via vi.stubGlobal
- Files changed: src/lib/erp/types.ts, src/lib/erp/odoo-adapter.ts, src/lib/erp/adapter-factory.ts, src/tests/odoo-adapter.test.ts
- **Learnings for future iterations:**
  - TypeScript circular type alias error: use intermediate interface (XmlRpcObject) to break recursion
  - vi.stubGlobal('fetch', mockFn) + afterEach vi.unstubAllGlobals() is the pattern for mocking fetch in vitest node env
  - authenticate() is private but tested indirectly via first fetch call URL + uid presence in second call body
---
